# --- Aliases ---
# Add a backslash before a command to use it unaltered

alias grep="grep --color=auto"
{{- if lookPath "kubectl" }}
alias k='kubectl'
{{- end }}
alias sts='aws sts get-caller-identity'
